
===== TRIAGE 2026-09-17 01:14:59 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m✱ [0mGlob "**/scope.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/avatarux-studios*.md"[90m 1 match[0m
I need to examine the repository to find the scope file and any hunt bot output. Let me check what's available.
[0m→ [0mRead probe-results.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead inventory/avatarux-studios.md
Let me check the probe results and leads files to find the actual leads to triage.
[0m→ [0mRead leads
[0m→ [0mRead triage-output.txt
[0m→ [0mRead triage-prompt.txt
Let me check the leads directory and triage output for actual leads to evaluate.
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
Let me read all the lead files to extract the actual leads for triage.
[0m→ [0mRead scope.yml
[0m→ [0mRead knowledge
[0m→ [0mRead knowledge/index.md
Now I have the full picture. Let me check the scope's out-of-scope list once more, then produce the triage.
I now have all the data needed for a complete 7-Question Gate triage. Here is the verdict for every distinct lead extracted from the hunt bot output:

---

## TRIAGE OUTPUT — AvatarUX Studios Bug Bounty

---

### LEAD 1: cpanel.avatarux.com — Cloudflare 1001 Dangling DNS (Subdomain Takeover Candidate)

| Q | Answer |
|---|--------|
| Q1 In scope? | Yes — avatarux.com is the program root domain. |
| Q2 Attacker reachable? | Yes — Cloudflare 1001 serves a public error page on port 443. |
| Q3 Real impact? | Yes — full subdomain takeover = phishing, credential theft, XSS on *.avatarux.com. |
| Q4 Provable non-invasively? | Yes — GET https://cpanel.avatarux.com/ returns Cloudflare 1001; dig confirms CNAME → avatarux.com → 162.159.136.54 (Cloudflare edge). |
| Q5 Novel? | Yes — no prior report found in scope. |
| Q6 On always-rejected list? | No — not banner/grabbing, not TLS best practice. |
| Q7 Reasonable triager? | No — NS/SOA confirms apex Bluehost delegation (ns1/ns2.bluehost.com), no separate claimable CNAME for cpanel; takeover mechanism is unproven and downgraded to monitoring. Cannot demonstrate exploitability. |

**Verdict: HOLD**
Cloudflare 1001 persists 48h+ but Bluehost apex delegation blocks standard Cloudflare zone claim. Requires DNS owner verification before reporting. Downgraded from actionable to monitoring-only.

---

### LEAD 2: help.desk.avatarux.com — Atlassian JSM Tenant ID / Org ID Exposure (Portals 4–100)

| Q | Answer |
|---|--------|
| Q1 In scope? | Yes — Atlassian helpdesk is AvatarUX infrastructure. |
| Q2 Attacker reachable? | Yes — portals 4–100 return HTTP 200 with page source leaking tenant-id, atlassianOrgId, Statsig env/shard. |
| Q3 Real impact? | Limited — tenant UUIDs and Atlassian org IDs are informational; no cross-tenant data access confirmed. Atlassian Edge blocks anonymous REST enumeration (303/404). |
| Q4 Provable non-invasively? | Yes — GET https://help.desk.avatarux.com/servicedesk/customer/portal/4 returns 200 with tenant-id in HTML source. |
| Q5 Novel? | Partially — tenant IDs in JSM HTML source is a known Atlassian behavior, not unique to AvatarUX. |
| Q6 On always-rejected list? | Close — "Descriptive error messages or headers" is out of scope; however, this is structured config disclosure in HTML, not a stack trace. |
| Q7 Reasonable triager? | Borderline — informational leak with no demonstrated cross-tenant impact; likely informational triage, not accepted as a vulnerability. |

**Verdict: HOLD**
Passive config leak confirmed stable across 96+ portals, but Atlassian Edge blocks data access. No demonstrated security impact beyond reconnaissance. Submit only if combined with a concrete cross-tenant access PoC.

---

### LEAD 3: affiliates.betpanda.io — IDOR on /rest/player/uid/{id} (AUTH_HELPED)

| Q | Answer |
|---|--------|
| Q1 In scope? | Yes — BetPanda is an in-scope brand. |
| Q2 Attacker reachable? | Partially — endpoint is auth-gated (401 unauthenticated); any authenticated affiliate can reach it. |
| Q3 Real impact? | Yes — if authenticated affiliate can supply arbitrary player UIDs, cross-affiliate player PII, balance, and transaction data would leak. |
| Q4 Provable non-invasively? | No — requires valid affiliate session to test. 401 confirmed; no unauthorized data access demonstrated. |
| Q5 Novel? | Yes — IDOR pattern on user-supplied path parameter is not trivially common. |
| Q6 On always-rejected list? | No. |
| Q7 Reasonable triager? | No — cannot demonstrate vulnerability without authentication. Pattern alone is not a reportable finding. |

**Verdict: INVALID (insufficient proof)**
Endpoint exists and is auth-gated; IDOR hypothesis is unverified. Requires a second authenticated affiliate session to demonstrate cross-user data access. Park until AUTH_HELPED proof is obtained.

---

### LEAD 4: affiliates.betpanda.io — /rest/public/config Information Disclosure

| Q | Answer |
|---|--------|
| Q1 In scope? | Yes — BetPanda is an in-scope brand. |
| Q2 Attacker reachable? | Yes — GET /rest/public/config returns 200 with operatorId, Strapi CMS URL, support email, currency list. No auth required. |
| Q3 Real impact? | Limited — operatorId=1, support email, CMS path are operational details, not credentials or PII. |
| Q4 Provable non-invasively? | Yes — single unauthenticated GET. |
| Q5 Novel? | No — /rest/public/config is a standard Strapi CMS endpoint; vendor behavior, not AvatarUX-specific. |
| Q6 On always-rejected list? | Borderline — "Disclosure of known public files or directories" is out of scope; this is a standard CMS endpoint. |
| Q7 Reasonable triager? | No — standard CMS config endpoint leaking non-sensitive operational info. Informational at best. |

**Verdict: INVALID**
Standard Strapi CMS public config endpoint; leaking non-sensitive operational data (operatorId, support email). Not a security vulnerability.

---

### LEAD 5: autoconfig.avatarux.com — Mail Server Config Disclosure (IMAP/SMTP Password-Cleartext)

| Q | Answer |
|---|--------|
| Q1 In scope? | Yes — avatarux.com subdomain. |
| Q2 Attacker reachable? | Yes — GET /autoconfig/v1.1/ returns XML with mail.avatarux.com:993/465 and password-cleartext auth. |
| Q3 Real impact? | Stale — mail host 301→WordPress/Bluehost (not a mail server). Config is legacy/dead. |
| Q4 Provable non-invasively? | Yes — single unauthenticated GET. |
| Q5 Novel? | No — Thunderbird autoconfig is standard; stale mail configs are common. |
| Q6 On always-rejected list? | **Yes** — "Mail configuration issues including SPF, DKIM, DMARC settings" is explicitly out of scope. |
| Q7 Reasonable triager? | No — out of scope per program rules. |

**Verdict: INVALID**
Explicitly out of scope (mail configuration issues). Stale config pointing to a non-functional host.

---

### LEAD 6: betpandacasino.io — OPTIONS /rest/user/authenticate Header Leak (x-site-name-id)

| Q | Answer |
|---|--------|
| Q1 In scope? | Yes — BetPanda casino is in-scope brand. |
| Q2 Attacker reachable? | Yes — OPTIONS returns 200 with Access-Control-Allow-Headers listing x-site-name-id. |
| Q3 Real impact? | No — ACAO is pinned to correct origin (https://betpandacasino.io); x-site-name-id is echoed as betpandacasino_io; no tenant switch demonstrated; header is informational only. |
| Q4 Provable non-invasively? | Yes — single unauthenticated OPTIONS. |
| Q5 Novel? | No — CORS header inspection is routine. |
| Q6 On always-rejected list? | **Yes** — "OPTIONS / TRACE HTTP method enabled" is explicitly out of scope. |
| Q7 Reasonable triager? | No — out of scope per program rules; no security impact demonstrated. |

**Verdict: INVALID**
Explicitly out of scope (OPTIONS method). CORS pinned correctly; no tenant isolation bypass demonstrated.

---

### LEAD 7: betpandacasino.io — /rest/user/details Unauthenticated Data Model Disclosure

| Q | Answer |
|---|--------|
| Q1 In scope? | Yes — BetPanda casino is in-scope brand. |
| Q2 Attacker reachable? | Yes — GET /rest/user/details returns 200 with loggedIn, country, kycVerified, currentLevel, blockedStatus. No auth. |
| Q3 Real impact? | Limited — data model/state, not PII; no user-specific data without auth. |
| Q4 Provable non-invasively? | Yes — single unauthenticated GET. |
| Q5 Novel? | No — unauthenticated endpoint returning empty/default state is common Spring Boot scaffold. |
| Q6 On always-rejected list? | Borderline — "Descriptive error messages or headers" could apply. |
| Q7 Reasonable triager? | No — returns empty/default user state model, not actual user data. Informational only. |

**Verdict: INVALID**
Returns empty/default user state model without auth; no PII or sensitive data exposed. Informational at best.

---

## SUMMARY

| # | Lead | Verdict | Action |
|---|------|---------|--------|
| 1 | cpanel.avatarux.com subdomain takeover | **HOLD** | Monitoring; Bluehost delegation blocks standard claim. Needs DNS owner verification. |
| 2 | help.desk.avatarux.com tenant ID exposure | **HOLD** | Informational; submit only with cross-tenant access PoC. |
| 3 | affiliates.betpanda.io IDOR /player/uid/{id} | **INVALID** | Insufficient proof; auth-gated, no cross-user data access demonstrated. |
| 4 | affiliates.betpanda.io /rest/public/config leak | **INVALID** | Standard CMS endpoint; non-sensitive operational data. |
| 5 | autoconfig.avatarux.com mail config | **INVALID** | Out of scope (mail config); stale/dead host. |
| 6 | betpandacasino.io OPTIONS header leak | **INVALID** | Out of scope (OPTIONS method); no impact. |
| 7 | betpandacasino.io /rest/user/details | **INVALID** | Empty state model; no PII or sensitive data. |

**Bottom line:** No leads from this cycle meet the bar for a VALID report. The two HOLD findings (cpanel takeover, tenant ID exposure) are real observations but lack sufficient proof or exploitability for submission. The IDOR on affiliates.betpanda.io is the most promising lead but requires a credentialed test to validate.
