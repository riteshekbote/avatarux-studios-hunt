
===== TRIAGE 2026-09-13 19:00:05 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m→ [0mRead scope.yml
[0m→ [0mRead probe-results.md
[0m→ [0mRead leads
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead triage-output.txt
The `LEADS:` section in your prompt is empty — no specific leads were passed. I've extracted the distinct leads from the hunt bot's lead files (`lead-bigpickle.md`, `lead-nemotron3.md`) and `probe-results.md`. Here is the triage:

---

## LEAD 1: cpanel.avatarux.com — Cloudflare 1001 DNS Subdomain Takeover

| Q | Answer |
|---|--------|
| Q1 Scope | YES — `*.avatarux.com` falls under "all infrastructure operated by AvatarUX Studios" in scope.yml |
| Q2 Reachable | YES — publicly resolves, HTTP 443 returns Cloudflare error 1001 (DNS to prohibited IP); CNAME chain: cpanel → avatarux.com → 162.159.136.54 (CF edge). No auth needed to observe. |
| Q3 Impact | YES — dangling DNS record on CF zone is a classic subdomain takeover vector; attacker could serve phishing/XSS on `*.avatarux.com` |
| Q4 Provable | PARTIAL — Cloudflare 1001 is observable passively via GET. But full takeover requires verifying orphaned CF zone status (crt.sh, DNS history) which is blocked in probe results. Cannot confirm claimability without active zone check. |
| Q5 Novel | YES — Cloudflare 1001 on cpanel.avatarux.com is consistently observed across 20+ cycles (stable >48h). No evidence of prior public report. |
| Q6 Always-rejected | NO — not on always-rejected list |
| Q7 Reasonable triager | YES — Cloudflare 1001 with stable dangling DNS is accepted by most triagers as a valid subdomain takeover lead |

**Verdict: HOLD**
*Reason: High-confidence subdomain takeover indicator (CF 1001, 48h stable). Cannot confirm claimability without zone ownership check. Needs verification step before submission.*

**Proof steps:** GET `https://cpanel.avatarux.com/` to confirm 1001 persists; query crt.sh for `%.avatarux.com` to verify certificate status; check DNS history for CNAME changes.

**Impact:** CRITICAL — full subdomain takeover under `*.avatarux.com` (phishing, credential theft, XSS injection)

**CVSS 3.1:** 9.0 (AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:N) — pending claimability confirmation

**Channel:** bugs.olivermaicher.eu (report + PoC + contact + payment)

---

## LEAD 2: affiliates.betpanda.io — IDOR on /rest/player/uid/{id}

| Q | Answer |
|---|--------|
| Q1 Scope | YES — BetPanda is an in-scope brand; affiliates.betpanda.io is confirmed live infrastructure |
| Q2 Reachable | YES — publicly accessible Vite SPA; API backend co-hosted at `/rest/*` (same-origin); endpoints return 401 without auth but are HTTP-reachable |
| Q3 Impact | YES — if authenticated affiliate can supply arbitrary player UIDs, cross-affiliate PII exposure and commission theft is possible |
| Q4 Provable | NO (currently) — endpoint requires authentication (`withCredentials: true`, 401 unauthenticated). No proof of IDOR possible without an auth session. Bot cannot create accounts per scope rules (account_creation: restricted). |
| Q5 Novel | UNLIKELY — `/rest/player/uid/{id}` is a standard pattern in gaming affiliate platforms; IDOR on such endpoints is a well-known class |
| Q6 Always-rejected | NO |
| Q7 Reasonable triager | NO — triager would reject a speculative IDOR without authenticated PoC. Bot explicitly notes "needs auth session" and "blocked on API URL discovery" then later "blocked on auth session." |

**Verdict: INVALID**
*Reason: Speculative IDOR without authenticated PoC. Endpoint returns 401; no evidence of authorization bypass or cross-affiliate data access. Bot cannot create accounts per program rules. Scanner output without exploit demonstration is explicitly rejected by program policy.*

**Channel:** N/A (do not submit)

---

## LEAD 3: affiliates.betpanda.io — /rest/public/config Information Disclosure

| Q | Answer |
|---|--------|
| Q1 Scope | YES — BetPanda in-scope brand |
| Q2 Reachable | YES — public endpoint, no auth required |
| Q3 Impact | NO — /rest/public/config leaking operatorId, Strapi CMS config, support email, and currency list is **intentionally public** configuration endpoint. This is normal for SPAs that need runtime config before auth. No secrets, no PII, no credentials leaked. |
| Q4 Provable | YES — simple GET returns JSON config |
| Q5 Novel | NO — public config endpoints are standard SPA architecture |
| Q6 Always-rejected | YES — falls under "Descriptive error messages or headers" and "Disclosure of known public files or directories" in out_of_scope |
| Q7 Reasonable triager | NO — leaking operatorId=1 and currency list is not a security issue |

**Verdict: INVALID**
*Reason: Intentionally public SPA runtime configuration. operatorId, CMS type, and currency list are not sensitive data. Out-of-scope per "Disclosure of known public files or directories."*

**Channel:** N/A

---

## LEAD 4: affiliates.betpanda.io — Password Reset Enumeration (/rest/public/recover-password/email/{email})

| Q | Answer |
|---|--------|
| Q1 Scope | YES — BetPanda in-scope brand |
| Q2 Reachable | YES — public endpoint, no auth required |
| Q3 Impact | UNCLEAR — returns 204 for all emails. Bot speculates timing differential could enable enumeration, but notes "rate limiting unverified." Without timing proof, this is speculative. |
| Q4 Provable | PARTIAL — endpoint is reachable, but proving enumeration requires measuring timing differentials across valid/invalid emails, which bot did not do. |
| Q5 Novel | NO — password reset enumeration is a well-known class; 204-only response is actually a good practice (no info leak in body) |
| Q6 Always-rejected | YES — "Password and account recovery policies" is explicitly out-of-scope. Also "Username enumeration based on login or forgot password pages" is out-of-scope. |
| Q7 Reasonable triager | NO — program explicitly rejects password/account recovery policy issues and username enumeration |

**Verdict: INVALID**
*Reason: Password reset enumeration is explicitly out-of-scope per program policy ("Password and account recovery policies" + "Username enumeration"). 204-only response is a correct defensive pattern.*

**Channel:** N/A

---

## LEAD 5: help.desk.avatarux.com — Atlassian JSM Tenant ID Enumeration

| Q | Answer |
|---|--------|
| Q1 Scope | YES — Atlassian infrastructure operated by AvatarUX |
| Q2 Reachable | YES — public HTTP 200 on root, 303 on portal, REST endpoints return 401/404 |
| Q3 Impact | PARTIAL — bot claims tenant IDs visible in HTML page source `dynamic_configs` JSON. However, tenant IDs in a SaaS product's frontend JS are normal and expected (they're not secrets). No cross-tenant data access was demonstrated. |
| Q4 Provable | PARTIAL — bot extracted tenant IDs from HTML, but never demonstrated they can be used for unauthorized API access or cross-tenant reads. All REST endpoints tested returned 401 or 404. |
| Q5 Novel | NO — Atlassian tenant IDs in frontend JS are standard JSM behavior; this is not a vulnerability |
| Q6 Always-rejected | Borderline — "Descriptive error messages or headers" and "Disclosure of known public files or directories" are out-of-scope |
| Q7 Reasonable triager | NO — leaked tenant IDs in frontend config without demonstrated cross-tenant access is not accepted |

**Verdict: INVALID**
*Reason: Atlassian tenant IDs in frontend JS config are expected SaaS behavior, not a security issue. No cross-tenant data access demonstrated. REST API consistently returns 401 (proper auth enforcement).*

**Channel:** N/A

---

## LEAD 6: cpcalendars.avatarux.com — HTTP 500 Information Disclosure

| Q | Answer |
|---|--------|
| Q1 Scope | YES — *.avatarux.com in scope |
| Q2 Reachable | YES — HTTP 500 response observed |
| Q3 Impact | NO — HTTP 500 on cPanel calendar/contacts subdomains is a known benign response when the feature is disabled. No sensitive data in response. Bot itself marked this "PARKED" with confidence 45. |
| Q4 Provable | YES — GET returns 500 |
| Q5 Novel | NO — this is a standard cPanel disabled-service response |
| Q6 Always-rejected | YES — "Descriptive error messages or headers" is explicitly out-of-scope |
| Q7 Reasonable triager | NO — rejected by the bot itself and would be rejected by triager |

**Verdict: INVALID**
*Reason: HTTP 500 on disabled cPanel services is a known benign response. Out-of-scope per "Descriptive error messages or headers." Bot self-rejected at confidence 45.*

**Channel:** N/A

---

## LEAD 7: betpandacasino.io — Spring Boot Debug Surface (/rest/user)

| Q | Answer |
|---|--------|
| Q1 Scope | YES — BetPanda casino in scope |
| Q2 Reachable | YES — GET /rest/user returns 200 "User Resource" (text/plain) |
| Q3 Impact | NO — "User Resource" is a Spring Boot scaffold placeholder, not an actual endpoint returning data. Bot notes "20 paths probed (all 404)" and confidence is 45. No data exposure, no auth bypass. |
| Q4 Provable | YES — GET returns static text |
| Q5 Novel | NO — Spring Boot scaffolding in production is a minor misconfig, not a vulnerability |
| Q6 Always-rejected | Borderline — falls under informational findings |
| Q7 Reasonable triager | NO — a static "User Resource" string with no data is not a reportable issue |

**Verdict: INVALID**
*Reason: Spring Boot placeholder text with no data exposure. 20+ paths probed returned 404. No security impact from a static text response.*

**Channel:** N/A

---

## LEAD 8: cable.betpanda.io — Custom WebSocket Service

| Q | Answer |
|---|--------|
| Q1 Scope | YES — BetPanda in-scope brand |
| Q2 Reachable | YES — HTTP 200, "BB CABLE" banner, Cloudflare-fronted |
| Q3 Impact | UNCLEAR — WS endpoints /socket /ws /events all return 404. No data exposure found. |
| Q4 Provable | PARTIAL — service is reachable but no working endpoint discovered. Bot notes "no concrete endpoint discovered, only root path known." |
| Q5 Novel | UNLIKELY — Node WebSocket services are common; no vulnerability found |
| Q6 Always-rejected | NO |
| Q7 Reasonable triager | NO — a service that returns 404 on all probed paths has no demonstrated vulnerability |

**Verdict: INVALID**
*Reason: Service reachable but all probed WebSocket paths return 404. No data exposure or auth bypass demonstrated. Parked by bot at confidence 45 due to insufficient evidence.*

**Channel:** N/A

---

## Summary

| # | Lead | Verdict | CVSS | Action |
|---|------|---------|------|--------|
| 1 | cpanel.avatarux.com CF 1001 Takeover | **HOLD** | 9.0* | Needs zone claimability verification |
| 2 | BetPanda IDOR /player/uid/{id} | **INVALID** | — | No auth session, speculative |
| 3 | BetPanda /rest/public/config leak | **INVALID** | — | Public config, not sensitive |
| 4 | BetPanda password reset enum | **INVALID** | — | Out-of-scope per policy |
| 5 | Atlassian JSM tenant ID enum | **INVALID** | — | Expected SaaS behavior |
| 6 | cpcalendars HTTP 500 | **INVALID** | — | Disabled feature, out-of-scope |
| 7 | betpandacasino Spring Boot stub | **INVALID** | — | No data, placeholder text |
| 8 | cable.betpanda.io WS service | **INVALID** | — | All paths 404, no finding |

**Only 1 actionable lead (LEAD 1) exists.** It needs a verification step (zone ownership check) before it can be escalated to VALID. All others lack security impact or are out-of-scope.
